/* Fichier d’exemple non branché sur le routeur principal. */
import React from "react";

const Home = () => (
  <div>
    <h1>Zen Coach Web</h1>
    <p>La page d’accueil est rendue par LandingPage.</p>
  </div>
);

export default Home;
